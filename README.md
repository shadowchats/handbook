# Shadowchats.Handbook

Справочник по стандартам, процессам и онбордингу для проектов **Shadowchats**.  

⚠️ Обратите внимание: автор постоянно дорабатывает стандарты, поэтому старые репозитории могут частично им не соответствовать.

## 📄 License

Shadowchats is free software licensed under the **GNU Affero General Public License v3.0**.

This means you can:
- ✅ Use the software for any purpose
- ✅ Study and modify the source code
- ✅ Distribute the software
- ✅ Distribute your modifications

**Important requirements:**
- 🔒 Any modifications must also be licensed under AGPL v3
- 📤 If you run a modified version on a server, you must provide source code to users
- 📝 You must preserve copyright notices and license information

See [LICENSE](LICENSE) for full details.

### Why AGPL v3?
We chose AGPL v3 to ensure that Shadowchats remains free and open source forever, even when used as a network service. This protects users' rights to access, study, and modify the software that handles their private communications.

## 📄 [COPYRIGHT](COPYRIGHT).

## 📧 Contact

For questions about licensing or to request source code:
- Email: lenya.dorovskoy@mail.ru
- GitHub repository: https://github.com/0ne290/Shadowchats.Handbook
- Author's GitHub: https://github.com/0ne290

## Codestyle

- Переиспользовать файлы `.gitignore`, `LICENSE`, `COPYRIGHT` и `README.md` из данного репозитория (допускаются изменения, кроме `LICENSE` и `COPYRIGHT`).  
- Во всех файлах, где это возможно, добавлять шапку-комментарий или фиктивное поле с копирайтом (см. `COPYRIGHT`).  
- **Статические данные** инициализировать в классе; **экземплярные** (включая неизменяемые) — в конструкторе.  
- **Первичные конструкторы** не использовать.  
- **Классы данных** (`DTO`, `Message`, `Result`, `Config`, `Info` и т. д.) определять как `record` со свойствами `required { get; init; }` либо менее строгими.  
- **Классы, сохраняемые в БД** (`ValueObject`, `Entity`, `AggregateRoot` и т. д.) не должны содержать сохраняемых зависимостей.  
  - Если классу нужна зависимость, внедрять её в метод, вызываемый из слоя **Application**.  

## Архитектурные принципы

- Использовать **DDD**, **CQRS** и **Event Driven** — обязательные подходы.  
- За основу брать решение `Shadowchats.Template.sln` из данного репозитория.  
- **Unit Testing** обязателен для слоёв **Domain** и **Application** (особенно `UseCaseHandler`-ов).  
- Желательно тестировать важный инфраструктурный код, в чьей корректности есть сомнения (например, самописное хеширование паролей).

## Порты

| № | Технология    | Протокол                      | Порт      |
| - | ------------- | ----------------------------- | --------- |
| 1 | **REST API**  | HTTP/1.1                      | **8080**  |
| 2 | **gRPC**      | HTTP/2 (H2C)                  | **50051** |
| 3 | **gRPC-Web**  | HTTP/1.1 (через proxy/bridge) | **8081**  |
| 4 | **WebSocket** | HTTP/1.1 (ws://)              | **6001**  |
| 5 | **GraphQL**   | HTTP/1.1                      | **7000**  |

## Флоу ручного E2E-тестирования кода

1. До создания PR: локально:
    1. Изолированно.
    2. Опционально: интегрированно.
2. На деве.

## Виды ручного E2E-тестирования кода

1. Локальное изолированное:
    1. В репозитории каждого микросервиса лежит файл `docker-compose.yaml` именно для этого вида тестирования - этот файл поднимает сам микросервис и все зависимости, прямо используемые им.
    2. `docker compose up --build`.
    3. Опционально, если надо накатить миграции: `docker compose run --rm placeholder-service dotnet Shadowchats.Placeholder.Presentation.dll migrate`.
2. Локальное интегрированное: поднимаешь в minikube K8s-кластер со всей системой, настраиваешь CI/CD и тестируешь. Подробную инструкцию смотри в [этом](https://github.com/shadowchats/deploy/tree/master) репозитории.
3. На деве: где деньги, Лебовски?

# 🧩 Микросервисы и DDD — корректная трактовка

## 1. Ключевой принцип

> 💡 **Bounded Context — это граница модели, языка и инвариантов, а не деплоя.**

- Контекст определяет *смысловую изоляцию*, а не техническую.  
- Микросервис — это всего лишь **технический носитель одного bounded context**.  
- В идеале, **1 bounded context = 1 микросервис**,  
  но не наоборот (не каждый микросервис — отдельный контекст).

---

## 2. Стратегии соотнесения контекстов и микросервисов

| Стратегия | Описание | Когда уместно | Опасность |
|------------|-----------|----------------|------------|
| 🧱 **Один контекст = один микросервис** | Классическая и чистая стратегия DDD | Почти всегда — базовый и правильный выбор | Нет |
| ⚙️ **Один контекст → несколько процессов (подсервисов)** | Один bounded context, несколько технических единиц (API, worker, projector) | Для масштабирования или разделения технических ролей | Только если контекст не разрывается по модели |
| ⚠️ **Несколько контекстов в одном микросервисе** | Несколько разных моделей и языков в одном деплое | Только на MVP-стадии, когда контексты ещё не оформлены | Ведёт к монолиту |
| 🚫 **Один контекст → микросервисы по сущностям** | Разделение “чаты”, “сообщения”, “пользователи” просто по таблицам | Никогда | Превращает систему в распределённый монолит |

---

## 3. Признаки принадлежности к одному bounded context

✅ Если выполняются эти признаки — значит, всё в одном контексте:

1. **Единый язык** (ubiquitous language) — термины и смысл совпадают.  
2. **Единая модель инвариантов** — общие бизнес-правила.  
3. **Общая жизненная цикличность данных** — объекты живут и умирают вместе.  
4. **Необходимость транзакций** между сущностями.  
5. **Общая команда разработки** и единый смысловой фокус.

❌ Если язык и инварианты разные — это уже разные контексты, независимо от схем БД или кода.

---

## 4. Общая БД внутри одного контекста

- Несколько процессов (микросервисов) **одного bounded context** могут использовать **одну и ту же базу данных**.
- Это не антипаттерн, если:
  - они делят **единую доменную модель**;
  - взаимодействуют **внутри транзакций**;
  - и не лезут в чужие контексты.  

> 🧠 *Антипаттерн — общая БД между разными bounded contexts, а не между процессами одного.*

---

## 5. Как реализовать многопроцессный контекст в Kubernetes

Три способа, не нарушая DDD:

| Вариант | Пример | Когда уместен |
|----------|---------|---------------|
| **Sidecar (несколько контейнеров в Pod-е)** | `chat-api` + `chat-processor` в одном Pod-е | Общие ресурсы, shared volume, единый lifecycle |
| **Несколько Deployments, один namespace** | Отдельно `chat-api`, `chat-worker`, `chat-realtime` с меткой `context: communication` | Независимое масштабирование, общая модель |
| **Один Helm Chart = один bounded context** | Чарт `communication/` содержит все манифесты процессов | CI/CD-уровень, единая единица поставки |

---

## 6. Границы взаимодействия между контекстами

- Взаимодействие только через **интеграционные события**, **API Gateway** или **domain events**, никогда напрямую в БД.  
- **Внутри контекста** — можно использовать ACID-транзакции.  
- **Между контекстами** — только eventual consistency и messaging.

---

## 7. Пример

### Bounded Context: **Коммуникация**

- **Домен:** чаты, сообщения, участники.  
- **Инварианты:**
  - Сообщение не может существовать без активного чата.  
  - Пользователь может писать только в чат, где он участник.  
- **Подсервисы:**
  - `chat-api` — REST/WebSocket API.  
  - `message-processor` — обработка и сохранение сообщений.  
  - `realtime-hub` — публикация событий в реальном времени.  
- **Общая БД:** `communication-db` (PostgreSQL, одна схема или несколько).  
- **Kubernetes:**
  - Namespace: `communication`
  - Helm Chart: `communication/`
  - В чарте несколько Deployments (`api`, `processor`, `hub`), одна база.

📊 **Инварианты и ACID сохраняются**, messaging только между контекстами (например, “Коммуникация” ↔ “Пользователи”).

---

## 8. Итоговые тезисы

- 🧠 **Микросервисы не выделяются по сущностям, а по смыслу контекста.**  
- 🧩 **Контекст — это единая модель, язык и инварианты.**  
- 💬 **Несколько процессов в одном контексте — допустимо.**  
- 🧱 **Одна база на контекст — нормально.**  
- 🔄 **Межконтекстное взаимодействие — только через события.**  
- 🚀 **DDD ограничивает смысл, а не способ деплоя.**

---
